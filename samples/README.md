# Samples

This folder stores external sample corpora used by experiments.

## Oxygen XML user guide sources

Checkout command:

```bash
./experiments/exp-000-baseline/scripts/setup_samples.sh
```

Materialized path (ignored by git):

- `samples/oxygenxml-userguide/`
